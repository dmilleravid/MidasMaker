{
  "name": "mobile",
  "version": "1.0.0",
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "test": "jest",
    "build": "npx tsc --noEmit",
    "build:android": "npx eas build --platform android --profile production --non-interactive",
    "build:ios": "npx eas build --platform ios --profile production --non-interactive",
    "build:preview:android": "npx eas build --platform android --profile preview --non-interactive",
    "build:preview:ios": "npx eas build --platform ios --profile preview --non-interactive",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web"
  },
  "dependencies": {
    "expo": "~53.0.22",
    "expo-router": "~5.1.5",
    "expo-status-bar": "~2.2.3",
    "nativewind": "^4.1.23",
    "react": "19.0.0",
    "react-native": "^0.79.5",
    "react-native-safe-area-context": "^5.4.0",
    "react-native-screens": "~4.11.1",
    "react-native-svg": "^15.12.1",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@types/jest": "^30.0.0",
    "@types/react": "~19.0.10",
    "babel-plugin-module-resolver": "^5.0.2",
    "jest-expo": "~53.0.10",
    "typescript": "~5.8.3"
  },
  "private": true
}
